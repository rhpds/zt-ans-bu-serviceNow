{
  "msg": "The task includes an option with an undefined variable.. - metadata:\n    name: \"{{ _instance_name }}-{{ guid }}\"\n  spec:\n    source:\n{% if _instance.image_type | default('pvc') == 'pvc' %}\n      pvc:\n        namespace: \"cnv-images\"\n        name: \"{{ _instance.image }}\"\n{% elif _instance.image_type | default('pvc') == 'registry' %}\n      registry:\n        url: \"{{ _instance.image }}\"\n{% elif _instance.image_type | default('pvc') in ['url','http'] %}\n      http:\n        url: \"{{ _instance.image }}\"\n{% endif %}\n    pvc:\n      accessModes:\n        - ReadWriteMany\n      volumeMode: Block\n      resources:\n        requests:\n          storage: \"{{ _instance.image_size }}\"\n: 'dict object' has no attribute 'image_size'\n\nThe error appears to be in '/runner/project/ansible/roles-infra/infra-openshift-cnv-resources/tasks/create_instance.yaml': line 95, column 3, but may\nbe elsewhere in the file depending on the exact syntax problem.\n\nThe offending line appears to be:\n\n\n- name: Create instance(s) \"{{ _instance.name }}\"\n  ^ here\nWe could be wrong, but this one looks like it might be an issue with\nmissing quotes. Always quote template expression brackets when they\nstart a value. For instance:\n\n    with_items:\n      - {{ foo }}\n\nShould be written as:\n\n    with_items:\n      - \"{{ foo }}\"\n",
  "_ansible_no_log": false,
}
